trigger:
- main

pool:
  vmImage: ubuntu-latest

variables:
  buildConfiguration: 'Release'

steps:
- script: dotnet build --configuration $(builConfiguration)
  displayName: 'dotnet buid $(buidConfiguration)'
